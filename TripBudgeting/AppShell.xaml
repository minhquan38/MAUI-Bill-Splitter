<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="TripBudgeting.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:view="clr-namespace:TripBudgeting.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="TripBudgeting">

    <ShellContent Title="Trip List" ContentTemplate="{DataTemplate view:TripListPage}" />
    <ShellContent Title="Trip Detail" ContentTemplate="{DataTemplate view:TripDetailPage}" />
    <ShellContent Title="Add Trip" ContentTemplate="{DataTemplate view:AddTripPage}" />

</Shell>
